import React from "react";
import Nav from '../Nav';

const Header = () => {
  return <header>
          <h1 className="page-title">React WPHeadless</h1>
          <Nav/>
        </header>;
};

export default Header;
